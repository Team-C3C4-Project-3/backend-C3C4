// import supertest from "supertest";
import { json } from "express";
import app from "./server";

let supertest = require('supertest')

describe("GET /recentrecs", () => {
    // test("response body is of type json", async () => {
    //     const response = await supertest(app).get("/recentrecs");
    //     expect(typeof response.body[0] === "object").toBe(true);
    // });
});